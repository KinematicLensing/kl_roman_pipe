# Sampler failure ledger

Permanent record of per-fit sampler failures in the ensemble pipeline: what
the diagnostic columns mean, the failure classes seen so far with their
signatures and evidence, and a per-campaign tally. Append to it whenever a
production-like run is read; never rewrite history. Handoffs in
`docs/sessions/` hold the narrative, this file holds the facts that must
survive sessions.

## Per-fit diagnostics (summary parquet rows, `results/<fit_id>.parquet`)

| column | meaning |
|---|---|
| `git_commit` | short sha of the kl_pipe checkout the worker ran at, `-dirty` when kl_pipe/ had uncommitted changes (absent on runs before 2026-09-13; a run whose jobs spanned a code change shows more than one value) |
| `max_rhat`, `min_ess` | escalation-gate quantities over all sampled parameters |
| `max_rhat_param`, `min_ess_param` | which parameter sets each (failure correlation) |
| `ess_g1`, `ess_g2` | shear ESS, the quantities the science uses |
| `n_attempts`, `escalated`, `escalation_mode`, `escalation_n_blocks`, `first_attempt_max_rhat`, `first_attempt_min_ess` | escalation history; `escalation_mode` is 'restart' (fresh warmup, donated metric), 'continue' (more draws from the warm chains in `escalation_n_blocks` blocks, first-attempt draws kept) or '' (no escalation) |
| `restart_reason`, `restart_recommended` | why more draws would not (or did not) rescue the first attempt: '' (marginal), 'rhat' (> `continue_rhat_max`), 'divergences' (> `continue_divergence_max`), 'blocks_exhausted' (continued to the block cap, still below the gate); `restart_recommended` marks fits to re-run fresh |
| `divergence_rate`, `mean_accept_prob` | NUTS health |
| `num_steps_total` | leapfrog steps summed over chains and draws (per-draw array in `chains/<fit_id>.npz['num_steps']`) |
| `precond_condition_number` | condition of the floored Laplace metric (`1/eig_floor` when the floor is active) |
| `precond_n_negative_eigenvalues` | negative eigenvalues of the unregularized scale-normalized MAP Hessian; > 0 means the optimizer stopped on a saddle or in a lower basin |
| `precond_min_eigenvalue_ratio` | min/max eigenvalue before flooring (negative when the above is > 0) |
| `n_map_starts_converged` | L-BFGS starts that met the convergence test |
| `map_pa_flip_margin` | negative-log-posterior margin of the MAP over the best optimization start that settled in the counter-rotating PA basin (`inf`: no start settled there; `nan`: half-turn PA prior, no such basin) |
| `map.<param>`, `map_minus_postmean_over_sigma.<param>` | MAP vs posterior mean, per parameter |
| `map_postmean_max_dev`, `map_postmean_max_dev_param` | largest \|MAP - posterior mean\| / sigma over the sampled parameters, and which one; healthy fits sit at 0.5-2.7 (64 fits, banks 983442 + 985872), a MAP the optimizer left in a wrong basin at 11-25 |
| `map_chi2`, `postmean_chi2`, `n_data` | -2 log L at the MAP and at the posterior mean (no data constant, so plain chi-squares) against the number of masked data pixels; a posterior stuck in a wrong basin shows a chi-square excess of hundreds to thousands (fit fcfb5651 in 985872: 1586 nats = 3172 in chi-square above the truth basin) |
| `post.<param>.q025`, `.q16`, `.q84`, `.q975` | central-interval edges from the pooled draws (theta_int on one contiguous branch about its median); with `truth.<param>` they give the coverage table (`diagnostics/coverage.csv`: fraction of truths inside the 68% / 95% intervals per cosi bin) |
| `truth_rank.<param>` | fraction of pooled draws below the truth (wrapped for theta_int), i.e. the simulation-based-calibration rank; uniform on [0, 1] for a calibrated posterior (`diagnostics/rank_hist_<param>.png`) |
| `flag_gate`, `flag_map_dev`, `flag_chi2_excess`, `flag_rotation_ambiguous`, `n_flags` | failure flags derived from the final row (`kl_pipe/ensemble/quality.py`): gate failed after escalation (`max_rhat` > `rhat_max` or `min_ess` < `ess_min`); `map_postmean_max_dev` > 5 (healthy 0.5-2.7, wrong-basin MAP 11-25); `postmean_chi2 - n_data` > 5 sqrt(2 n_data) (a chi-square with n_data degrees of freedom has standard deviation sqrt(2 n_data)); `map_pa_flip_margin` < 3 nats (the two rotation directions are competing modes). A missing or non-finite input never flags. Per-run counts appear in the bench records as `n_flag_*`; flagged fits in `diagnostics/flags.csv` |

Gate (production specs): `rhat_max` 1.05, `ess_min` 50, one escalation retry
(800/1000 warmup/samples, donated adapted metric; with `escalation.mode: auto`
a marginal first attempt is instead continued from its warm chains in blocks
of `continue_block` = 300 draws per chain, gate re-checked per block, at most
`continue_max_blocks` = 4 blocks). A fit that fails the gate after the retry
is kept and flagged in `status`/`collate` as catastrophic.

## Failure classes

| class | signature | evidence | status | candidate fix |
|---|---|---|---|---|
| A. Multimodal chains | first-attempt `max_rhat` >> 1.1 (2.6-2.9), `min_ess` 2-4, no divergences; chains sit in different basins | fit dacee1cd (noise_seed 1780535355) in every fp64 arm of the cosmos25_noise_ab_matched bank; escalation reaches only rhat 1.07 at 460k-670k steps; sets the 8-worker run wall | OPEN, the slow tail | mode enumeration: PA-flip counterpart descent + basin margin at the preconditioner; chain init spread across basins; report both modes |
| B. Saddle / lower-basin MAP | `precond_n_negative_eigenvalues` > 0; first attempt `min_ess` ~25, `max_rhat` ~1.08 | fit 95e70579 (n_neg 5, escape +1167 logpost -> n_neg 0, min_ess 24 -> 99, rhat 1.085 -> 1.048); fit 12eab959 (n_neg 4, coarse line scan finds no improving direction, sampling unaffected) | 2/16 at the production MAP (PA-stratified starts); the 2026-08-28 "7/16" count predates them | iterated escape (line scan along the most negative eigenvector + re-descent), opt-in stage in `laplace_preconditioner`; finer or 2-D scan for the residual class |
| C. Ridge geometry | steps/draw pinned at 63-127 (tree depth 6-7) on clean fits; directional curvature changes 30-400x within +/-1 sigma along the softest eigenvectors | all 16 bank fits (curvature_swing study 2026-09-07); metric changes (fd vs ad, escaped MAP) leave steps/draw unchanged | OPEN, the per-draw cost floor | sampler-layer bijective reparam informed by the MAP Hessian (spin-2 disk-frame shear, vcirc sin i); position-dependent metric; MAMS |
| D. Prior-wall regularization loss | flat shear prior: escalations 4 -> 7, steps +34%, median fit wall 17 -> 43 min, posteriors unchanged | flatprior arm 974729 vs 968810 | PARKED (Gaussian prior stays) | reparam first |
| E. Tree-depth cap | `max_tree_depth` 8: steps -3%, one extra catastrophic fit (rhat 1.29 / ess 12 after escalation, converged uncapped) | depth8 arm 976034 vs 968810 | REFUTED as a lever, do not adopt | none |
| F. Position-angle prior wall | truth `theta_int` within ~0.3 rad of the `Uniform(0, pi)` fit-prior walls; first-attempt `max_rhat` 1.07-2.9, `min_ess` 2-60; the counter-rotating solution (theta + pi) is outside the prior, so it appears as theta near the opposite wall with a compensating shear (fit dacee1cd: modes at theta 0.15-0.25 with g2 -0.19 / -0.24 and at 2.9 with g2 0.0, truth 3.04; log-posterior gap ~8 nats in favour of the truth mode) | census v1 (200 fits, July): fail rate 33% / 53% for wall distance < 0.15 / 0.15-0.3 rad vs 15% beyond 0.8 rad, median steps 2x, median theta pull 1.14 sigma vs ~0.5; 16-fit bank 968810: all 4 escalations among the 6 fits within 0.4 rad of a wall, 0 of 10 beyond 0.5 rad | FIXED: `fit.pa_prior: full_circle` is the default since 4a4a317 (`CircularUniform` prior, periodic sampling coordinate, PA starts over both rotation directions, `map_pa_flip_margin` column); A/B 983939 vs 968810: steps -30%, near-wall steps x0.38, tail fit 460k -> 104k first try | none; fits with `map_pa_flip_margin` < ~3 nats have a genuinely ambiguous rotation direction and the circle samples both modes |

## Infrastructure failures (not sampler pathologies)

| failure | signature | evidence | fix |
|---|---|---|---|
| fp32 escalation retry rejected the donor metric | `ValueError: init_inverse_mass_matrix must be symmetric` at `_run_fit_escalated` | fp32-ad arm 974798, all 4 escalating fits | eccfc62: donor symmetrized at working precision |
| AD Hessian OOM at pack 8 | `RESOURCE_EXHAUSTED ... 5.7 GiB ... jit__log_posterior_jittable` | fp64-ad arm 974797, 4/16 fits | eccfc62: one HVP per parameter |
| cuFFT single-precision plan failure | `Failed to make cuFFT batched plan: 5` at the first fp32 FFT, every fit, within a minute | container nightly on all nodes tried (2026-09-03); release jax 0.11.1 on c642-032 (976033) but not on c642-002, c642-042, c642-072 | OPEN: node-dependent; fp32 production needs a startup probe + requeue |
| end-of-run log-posterior chunk OOM at 8 packed workers | `RESOURCE_EXHAUSTED: Out of memory while trying to allocate 4.15-5.70GiB ... [executable_name='jit__log_posterior_jittable']` raised from `_batched_log_posterior_chunked` after `mcmc.run` returned; the fit's sampling is complete and lost | census v2 job 991629: 20 of 64 claimed fits; the 12-worker arm 991151: 22 of 32 (read at the time as a packing limit) | 9cbf221: chunk 256 -> 16 draws (the vmapped render carries ~15 MB of intermediates per draw; the NUTS gradient itself needs 0.16 GiB), so the transient is ~0.3 GiB; fits re-queued with `reclaim --clear-failed` |
| JAX persistent-cache race between jobs on one node | `jax.errors.JaxRuntimeError: NOT_FOUND: .../xla_gpu_per_fusion_autotune_cache_dir/tmp/tmp_per_fusion_cache_...textproto` inside `mcmc.run`, fit marked failed with NaN summary | w400 remainder job 986384 and w200 job 986080 ran concurrently on c642-001 with the same `jax_cache_rel_0.11.1` directory; 1 fit of 32 | OPEN: per-job autotune cache subdirectory (or serialize jobs per node); re-run the failed fit |

## Campaign tally

| date | run | code | N | escalated | catastrophic | notes |
|---|---|---|---|---|---|---|
| 2026-08-06 | census-era baseline, 4 workers | Aug-6 | 16 | 6 | 0 | 11.93 h sum wall |
| 2026-09-05 | 968810 fp64 fd (reference) | d2fcdd6 | 16 | 4 | 0 | 7.45 h; one class-A fit sets the run wall |
| 2026-09-05 | 968857 fp64 local window | 803757c | 16 | 4 | 0 | posteriors identical to reference |
| 2026-09-06 | 974729 fp64 flat shear prior | 803757c | 16 | 7 | 0 | class D |
| 2026-09-06 | 974797 fp64 AD Hessian | 232f1cf | 12/16 | 2 | 0 | 4 OOM (infra) |
| 2026-09-06 | 974798 fp32 AD Hessian | 232f1cf | 12/16 | 0 | 0 | 4 died at escalation (infra) |
| 2026-09-06 | 976032 fp64 AD Hessian | eccfc62 | 16 | 2 | 0 | agrees with fd on all 16 |
| 2026-09-06 | 976033 fp32 AD Hessian | eccfc62 | 0/16 | - | - | cuFFT plan failure, node c642-032 |
| 2026-09-06 | 976034 fp64 depth cap 8 | eccfc62 | 16 | 3 | 1 | class E |
| 2026-09-07 | 976765 fp32 AD Hessian | eccfc62 | 16 | 6 | 1 | node c642-042; shear means agree with fp64 (median 0.11 sigma), widths +3%; ms/step 0.79x but sum wall equal because of the extra escalations |
| 2026-09-07 | 976766 saddle-escape A/B, 7 fits x 2 arms | eccfc62 | 7 | - | - | class B prevalence 2/7; class A fit unchanged; steps/draw unchanged |
| 2026-09-08 | 983442 fp64 fd, 32-fit bank cosmos25_bank32 | a3af13c | 32 | 4 | 0 | first-pass 12.5%; theta_int sets min_ess in 16/32; near-wall median steps 107-132k vs 61k far; no negative MAP eigenvalues among the escalations |
| 2026-09-08 | 983939 fp64 fd, full-circle PA prior (class F fix) | 1a6f914 | 16 | 4 | 0 | vs 968810: steps 2.65M -> 1.85M, sum wall 7.45 -> 5.09 h; near-wall (6 fits) steps x0.38, escalations 4 -> 1, the tail fit 460k -> 104k steps first try; far-wall (10 fits) steps x1.11, escalations 0 -> 3 at first-attempt rhat 1.06-1.07; shear means agree (g1 max 0.16 sigma; g2 max 0.72 sigma on a near-wall fit, 0.12 far); flip margins 0.6-1396 nats, 2/16 below 2 nats |
| 2026-09-09 | 985582 fp64 fd, escalation mode auto (continue), same 16 fits as 983939 | 4a4a317 | 16 | 4 | 0 | first attempts bit-identical to 983939; all 4 escalations marginal (rhat 1.06-1.07, min_ess 63-130) -> continued 1000 draws/chain, all passed (rhat 1.004-1.009, min_ess 367-703); escalated fits wall 113 vs 140 min (x0.81) despite 1.6x more sampling steps (no 800-draw re-warmup); run wall 2662 vs 3175 s; posteriors agree with the restart arm (shear |dmean|/sigma <= 0.12, widths 0.91-1.05); the 1000-draw block was ~3x more than the gate needed, hence the block-wise continuation (continue_block 300, max 4) |
| 2026-09-09 | 985872 fp64 fd, cosmos25_bank32_w250 (n_warmup 250; also the first bank32 run at the full-circle PA prior default, so NOT a single-variable twin of 983442; matched reference w200 = job 986080) | 3c80b5a | 32 | 4 | 1 | first-pass 4/32 again but a different set: 3 of the 4 983442 failures (incl. 967ce510, tau 102) pass cleanly, 3 new fits fail (one at rhat 1.158; one still 1.080 after the restart); steps/draw on clean fits 63 -> 34, clean fit wall 10.9 -> 9.2 min, sum wall 9.45 -> 8.37 h; median first-attempt rhat 1.020 -> 1.023, worst tau 6.9 -> 6.3 (unchanged); per-chain adapted-metric spread measured directly from the saved matrices (window 100): x1.47 median along the softest direction, generalized-eigenvalue range 0.21-2.36 over the full matrix, and NO correlation with first-attempt rhat (rho -0.01); g2 widths x1.09 (circle prior, as in 983939) |
| 2026-09-10 | 986080 fp64 fd, cosmos25_bank32_w200 (matched reference: full-circle PA prior, n_warmup 200) | cbf2d0d | 32 | 2 | 0 | vs 983442 (half-turn): steps/draw on clean fits 58 -> 42, clean fit wall 12.8 -> 8.8 min, sum wall 9.45 -> 7.14 h, first-pass fail 4 -> 2; per-chain ridge-tau spread 3.6 -> 2.6; shear posteriors agree (med 0.06-0.12 sigma) |
| 2026-09-10 | 985873 + 986384 fp64 fd, cosmos25_bank32_w400 (n_warmup 400; timed out at 2 h with 30/32, remainder rerun) | 3c80b5a | 31/32 | 3 | 1 (infra) | fit 3ae35b04 lost to the JAX autotune-cache race below (infrastructure); steps/draw clean 32 but sum wall 9.67 h: one first attempt at rhat 1.71, one escalated fit 1.89M steps / 114 min (800/1000 restart); longer warmup does not lower the first-pass count; escalation restarts dominate cost |
| 2026-09-10 | 985912 fp64 fd, cosmos25_bank32_frozen (adapt_mass false; timed out at 2 h with 23/32; remainder job 987877) | cbf2d0d | 23/32 | 17 | ? | frozen Laplace metric: 17 first-pass escalations among 23 fits vs 2-4 with adaptation; clean fits fast (done-fit wall median 427 s) but the metric alone is not good enough; the July "adapt-mass is the cure" verdict stands under the circle prior |
| 2026-09-10 | 988356 fp64 fd, cosmos25_bank32_mapfix (fit.map_bounded + map_polish_steps 8 x 3 basins) | e73c019 | 32 | 3 | 0 | vs 986080: MAP dev max 1.2 (was 24.7 on g5_r90); grad norm < 1e-3 on 28/32; the reference g5_r90 posterior was in the counter-rotating mode on all 4 chains (v0 -104 vs truth +15) and passed the gate -- a silent wrong-mode fit the bounded MAP fixes; g8_r90 rotation direction genuinely ambiguous (0.14 nats), each run in a single opposite mode with identical shear; precond wall 22 -> 43 s median; sum wall 25.7k -> 29.4k s, dominated by the two cosi 0.054 fits whose MAP sits on the cosi 0.05 wall (grad norm 0.9-9, negative eigenvalue; 1.6-2.1x steps); first-pass 3/32 on fits disjoint from the reference's 2; shear posteriors agree (median 0.04-0.05 sigma, widths 0.99) |
| 2026-09-10 | 988824 fp64 fd, cosmos25_bank32_priorfloor (fit.eig_floor_mode prior, eig_floor 0.5) | e73c019 | 32 | 0 | 0 | vs 986080: 14/32 fits bit-identical (relative floor never clipped there); on the 18 edge-on fits steps 0.42-1.45x (median 1.00), both reference escalations pass first-pass at 0.42-0.44x steps; sum steps -10%, sum wall -9%, max fit wall 1791 vs 2955 s; posteriors agree (max 0.2 sigma, widths 1.00); condition number up to 2.4e5 unpegged with no step penalty; g5_r90 again silently in the counter-rotating mode (legacy MAP), rhat 1.011 |
| 2026-09-10 | 988826 fp64 fd, cosmos25_bank32_basininit (fit.chain_init map_basins, margin 20 nats; legacy unbounded MAP) | e73c019 | 31/32 | 8 | 0 (5 final gate fails) | TIMEOUT at 2 h; the unbounded search reports 3-12 'basins' per fit, mostly false-convergence stall points, so chains start at non-optima: 8 escalations vs 2, 5 still fail after restart (rhat 1.06-1.47, min_ess 4-21), sum wall +37%; passing fits agree with the reference; g5_r90 again counter-rotating (legacy MAP chi2 9240 vs 6110 truth basin). Chain-per-basin needs the bounded search first |
| 2026-09-11 | 990891 fp64 fd, cosmos25_bank32_robust (map_bounded + polish 8 x 3 + eig_floor_mode prior 0.5: the two single-knob winners together; the matched reference for arms on the new defaults) | ae63ae0 | 32 | 2 | 0 | vs 986080: the fixes compose. First-pass g15_r90 (rhat 1.20, ess 9) and g12_r0 (rhat 1.05, ess 66) vs g15_r90 + g5_r90 in the reference; g5_r90 in the truth basin (theta 1.33 vs 1.14, v0 -8.6 vs +15); posteriors agree (median 0.03-0.07 sigma, widths 1.00-1.01) and are identical to 988356 (max 0.21 sigma). Sum wall +9.4%, sum steps +8.7%, but 0.98 / 1.00 without the two truth-cosi-0.054 fits (g2_r0 234k, g2_r90 141k steps vs 98k / 61k; MAP on the cosi 0.05 wall, projected gradient 9.1 / 0.95, one floored eigenvalue). Preconditioner wall 42 s median. g8_r90 in the truth rotation mode this time (flip margin 0.14 nats; 986080 sampled theta + pi with the same shear). Face-on (truth cosi > 0.7, 8 fits) cosi pull median -0.38 sigma, min -1.04, unchanged: the cosi-wall arm 990979 tests it |
| 2026-09-11 | 990892 fp64 fd, cosmos25_bank32_robust_basins (chain_init map_basins, margin 20 nats, on the bounded search) | ae63ae0 | 32 | 11 | 0 (9 final gate fails) | REFUTED as a default vs 990891: 21/32 fits report 2-5 basins, 12 with a second basin within 20 nats got a chain there; 9 of those chains never join (rhat 1.06-2.7, min ESS 2-28), sum wall 1.58x, single-basin fits unchanged (steps 1.03x). The trapping basins sit 3-20 nats below the MAP (mass e^-3 to e^-20), so the rhat failure measures the trap, not the posterior. Within 3 nats: 3 fits (g8_r90 0.14, g13_r90 1.6, g2_r90 1.7); `map_basin_margin` from the ordinary map_jitter run identifies them, so the census flags margin < 3 nats instead of starting chains there |
| 2026-09-11 | 990979 fp64 fd, cosmos25_bank32_cosiwide (fit.cosi_prior_range [0.02, 1.0], draw range [0.05, 0.95]) | ae63ae0 | 29/32 | 5 (of 29) | 0 | TIMEOUT at 2 h, the three face-on fits g12_r0 / g12_r90 / g13_r90 unfinished. REFUTED as a cost lever and mostly as a bias fix: on the 29 matched fits the per-step cost rose 1.14x (the lower prior bound sets the worst-case k-space grid for every fit; preconditioner 1.26x), steps 1.05x, wall 1.23x, escalations 5 vs 1; the two truth-cosi-0.054 fits moved their MAP from the 0.05 wall to the 0.02 wall (projected gradient 13.6 / 1.9, steps 253k / 172k, now escalated); cosi posterior means moved toward truth by 0.03 (edge-on pull +0.60 -> +0.46, face-on -0.70 -> -0.48 on 5 fits), widths unchanged; shear posteriors identical (median 0.07 sigma, widths 0.98). Reading: for a thick disk (h/r 0.25) the observed axis ratio is flat in cos i below ~0.15, so edge-on cos i is prior-dominated at any lower bound and the mean pull is truncation of a flat likelihood, not a sampler bias. Keep the fit prior at the generating range for the census; a face-on-only widening [0.05, 1.0] costs no grid and is untested |
| 2026-09-12 | 991629 fp64 fd, cosmos25_census_v2 job 1 of ~4 (gh, 3 h, 8 workers, hard_first claim order) | c08b647 | 44/72 claimed | 6 | 0 (20 infra) | 20 fits died at the end-of-run log-posterior chunk (infra row above), 8 killed at the wall; the 44 are the hard tail by construction (truth cosi median 0.10, 37 below 0.3; line SNR 10-27): first-pass 6/44, 0 final fails, wall median 13.5 min (face-on 7 fits 29.7 min, 3 escalated), 4/44 posteriors in the counter-rotating mode (3 flagged at flip margin < 3 nats, one at 4.5 nats not flagged; shear pulls of the four within 1.1 sigma), 13/44 rotation-ambiguous, shear ESS < 200 in 32%; pulls g1 -0.07 +- 0.81, g2 +0.25 +- 0.71, cosi +0.02 +- 1.11; 68/95 coverage g1 0.70/0.98, g2 0.77/0.98, cosi 0.61/0.86, theta 0.66/0.86. Remaining jobs run at 9cbf221 |
| 2026-09-13 | 992869 fp64 fd, cosmos25_census_v2 job 2 (gh, 3 h, 8 workers, reclaim --clear-failed) | 9cbf221 | 77/85 claimed | 5 | 0 | 0 OOM after the chunk fix; 8 killed at the wall. GPU 8 x 3.0 GiB flat of 95.6, utilisation 100%, memory bandwidth 51%. Truth cosi median 0.15, line SNR median 14: first-pass 5/77, wall median 10.1 min (mean 14.4, max 50.9), 26.8 fits/node-hr, shear ESS < 200 in 27%, 13/77 rotation-ambiguous, 2 counter-rotating; pulls g1 +0.04 +- 0.80, g2 -0.16 +- 0.73, cosi +0.11 +- 0.80; 68/95 coverage g1 0.79/0.97, g2 0.87/0.99, cosi 0.77/0.99 |
| 2026-09-14 | 994372 fp64 fd, cosmos25_census_v2 job 3 (gh, 3 h, 8 workers) | 9cbf221 | 40/48 claimed | 10 | 1 | Throughput halved (13.3 fits/node-hr) as the face-on cosi 0.8 fits arrived (|cosi - 0.5| = 0.3 alongside cosi 0.2 in hard_first order): face-on fits take 2.2x the steps (median 132k vs 60k) and 13/37 escalate. First final gate failure: b01dbf1cb23ddb98 (cosi 0.80, SNR 21) restart-escalated at rhat 1.22 / ESS 15 and ended at rhat 1.45 / ESS 4. Of the 8 wall-killed, 520fbef1efe9d63d and 031e7fcc0a5840f2 (cosi 0.87 / 0.82) took the restart path (n_warmup 800, n_samples 1000, donated metric) at ~12 s/step, ~6 h per fit, so they cannot finish inside a 3 h job; both were also wall-killed in job 2 and will be again in jobs 4 and 5 unless the chain is patched: 2 workers x 3 h lost per job. Job totals at 161/400: escalated 21 (13%), 1 final fail, wall median 11.2 min, shear ESS < 200 in 34%, 34 rotation-ambiguous, 6 counter-rotating; pulls g1 -0.05 +- 0.80, g2 -0.03 +- 0.77, cosi +0.10 +- 0.88; 68/95 coverage g1 0.79/0.97, g2 0.83/0.99, cosi 0.72/0.95 |
| 2026-09-14 | 994373 fp64 fd, cosmos25_census_v2 job 4 (gh, 3 h, 8 workers) | 9cbf221 | 41/49 claimed | 5 | 0 | 8 killed at the wall (the two ~6 h restart fits 520fbef1efe9d63d / 031e7fcc0a5840f2 among them again). Truth cosi 0.19-0.80 (median 0.24), line SNR 10-27: first-pass 5/41 (4 continue, 1 restart), wall median 11.5 min, steps median 71k, 13.7 fits/node-hr, shear ESS < 200 in 37%; pulls g1 +0.19 +- 0.94, g2 -0.01 +- 0.89, cosi +0.28 +- 0.67; 68/95 coverage g1 0.71/0.95, g2 0.68/1.00, cosi 0.80/1.00 |
| 2026-09-15 | 994374 fp64 fd, cosmos25_census_v2 job 5 (gh, 8 h, 8 workers) | 9cbf221 | 113/121 claimed | 18 | 0 | 8 killed at the wall. Truth cosi 0.25-0.87 (median 0.38), line SNR 10-28: first-pass 18/113 (14 continue, 4 restart), wall median 11.7 min, steps median 70k, 14.1 fits/node-hr, shear ESS < 200 in 38%; pulls g1 -0.01 +- 0.98, g2 +0.01 +- 0.94, cosi +0.23 +- 1.10; 68/95 coverage g1 0.73/0.94, g2 0.75/0.95, cosi 0.69/0.96. Census at 315/400 (escalated 44, 14%); the 85 left are truth cosi 0.39-0.66, jobs 6-7 (998892/998893) queued |
| 2026-09-16 | 1001111 fp64 fd, cosmos25_bank32_robust_pooled (fit.warmup_metric pooled, 50 frozen stage-2 draws; run from the second clone at 7acd0d8) | 7acd0d8 | 29/32 | 2 | 0 | TIMEOUT at 2 h with 3 fits unfinished (completion job 1001735 chained). REFUTED at the wall-normalized gate vs 990891 on the 29 matched fits: sum fit wall 1.16x, median wall 0.97x, shear ESS median 238 vs 239, shear ESS per wall-hour 0.94x, per step 1.02x (1.33x on the 26 fits neither side escalated, but the stage-2 warmup replaces production steps so their shear ESS is 0.93x); posteriors agree (dmean/sigma 0.03-0.10, widths 0.96-0.99); first-pass 2 vs 1, escalations 2 vs 1, 0 final fails. Stage-2 warmup 7.1k steps median (max 62k) on top of 39.6k. warmup_metric stays adapted |
| 2026-09-11 | 991151 fp64 fd, cosmos25_bank32_robust_w12 (12 packed workers per GH200, dynamic allocation + MPS) | ae63ae0 | 32 | - | 22 | REFUTED: 22/32 fits died at RESOURCE_EXHAUSTED allocating 4.9-5.3 GiB for the compiled log-posterior; the 10 that ran took 1.36x the per-step time of the 8-worker run. 8 workers per node stays the production packing (July: 16 lost 2 of 16, 32 collapsed) |
| 2026-09-16 | 1002232 fp64 fd, cosmos25_census_v2_lsnr10x (line SNR x10, Xu+23 TF 0.049 dex, 60 cos i-stratified galaxies x ring pair; gb) | 442f1b7 | 120 | 6 | 0 | run wall 33 min, fit wall median 4.7 min, steps median 41k; sigma(g+) median 0.044 (0.044/0.053/0.046/0.038/0.031 edge..face), rel sigma(v sin i) 0.06-0.14 vs 0.20-0.31 at census, corr(vcirc, cos i) 0.43-0.60 for cos i > 0.5: the kinematic channel is on at x10 (round disk, h pinned) |
| 2026-09-16 | 1002233 fp64 fd, cosmos25_census_v2_lsnr3x (line SNR x3, same 120 fits) | 442f1b7 | 120 | 14 | 0 | run wall 96 min, fit wall median 5.9 min; sigma(g+) median 0.080, 1.1-1.4x better than census in every bin, from the sharper Halpha image rather than kinematics (rel sigma(v sin i) 0.10-0.12 below cos i 0.72, 0.21 face-on, corr 0.15-0.26): the marginal regime |
| 2026-09-17 | 1003195 fp64 fd, cosmos25_bank32_robust_pooled15 (pooled warmup metric, 15 stage-2 draws; gb) | 442f1b7 | 32 | 5 | 0 | REFUTED as a default vs 990891: clean-fit sum wall 0.94x but 5 vs 2 marginal first-pass fails (rhat 1.05-1.10 on g1/g2), sum fit wall 10.4 vs 7.5 h, accept 0.941 vs 0.935 (step size is fine; the window-end reset hypothesis is not supported). Pooled pays only under a never-restart escalation |
| 2026-09-17 | 1003205 fp64 fd, cosmos25_census_v2_faceon_open (ten widest-theta face-on census v2 galaxies x ring pair, cos i fit prior open to 1.0; chains saved for offline chart scoring; gb) | 442f1b7 | 20 | 9 | 0 | steps median 268k, fit wall median 23 min (1.31x the census twins), widths unchanged, cos i pull 0.00 +- 0.28 vs -0.28 +- 0.24 (n = 20). Offline chart scoring on these chains: the native (cos i, theta) chart wins 14/20 and every fit with sigma(theta) > 1 rad (Mardia 1.37 vs spin-1 2.21, min ESS 162 vs 74). Charts parked |
| 2026-09-17 | 1003245 fp64 fd, cosmos25_census_v2_lsnr10x_tf092 (x10 at the census TF width 0.092 dex, same 120 fits as 1002232; gb) | 442f1b7 | 120 | 3 | 0 | run wall 26 min; sigma(g+) ratio to 1002232: 0.98/0.96/0.89/0.93/0.91 edge..face (Fisher 1.00/1.00/0.83/0.86/0.78), face-on v sin i identical: TF width is third order even with the kinematic channel on |
| 2026-09-19 | 1008456 fp64 fd, cosmos25_census_v2_hfree (shared sampled thickness LN(0.25, 0.14 dex), prior == paint; 100 census v2 galaxies x ring pair; gb, 4 GPUs) | 7748b06 | 199/200 | 48 | 2 | run wall 3.2 h, fit wall median 12.0 min, steps median 93k (1.14x the pinned twins); 1 fit lost to the JAX per-fusion autotune-cache race (infra). vs census v2 fit by fit: sigma(g+) 1.06x (1.15x edge-on, 1.03x above cos i 0.5), all other widths within 5%; h posterior 0.91x its prior (0.82x edge-on), i.e. thickness is not measured at census depth; shear ranks uniform, h ranks mildly U-shaped (this sample's true h scatter is 0.163 dex vs the 0.14 prior). Headline census sigma(g+) 0.09-0.13 with sampled thickness |
| 2026-09-19 | 1008457 fp64 fd, cosmos25_census_v2_esc_continue (never restart, 8 continue blocks of 300, 90-min wall budget; the 56 census v2 galaxies with an escalated fit; gb) | 7748b06 | 112 | 36 | 8 | REFUTED as a blanket policy: gate pass 0.929 vs 0.973 on the same fits in census v2, catastrophic 8 vs 3; all 8 are vel.v0 chain splits (first-attempt rhat 1.13-1.73 on v0, min ESS 3-38, identical log-posterior per chain, line SNR 20-48: slow mixing along a flat nuisance, not modes); 4 of them ran zero blocks because the continue path declines already-broken chains. Census v2 restart cured 21 of 24 such first attempts. Steps 0.78x, raw wall 0.55x (gb vs gh), widths identical; budget_exhausted fired 4 times. Policy: continue for rhat <= 1.1, restart above |
| 2026-09-19 | 1008458 fp64 fd, cosmos25_census_v2_hfree_lsnr10x (sampled thickness at x10 line SNR, census TF 0.092; same 200 fits as 1008456; gb) | 7748b06 | 200 | 24 | 1 | run wall 1.0 h, fit wall median 7.4 min. vs the pinned-h x10 arm 1003245 (60 matched galaxies): sigma(g+) 1.46x (2.28x edge-on, 1.10x above cos i 0.7); vs 1008456 the x10 gain is 0.75x (0.57-0.60x above cos i 0.7, 0.83-0.85x below 0.5) where the round disk gave 0.48x. h starts to be measured (0.78x prior, 0.70x edge-on). 68% coverage 0.58 (g1) / 0.62 (cos i) but population ranks uniform (KS p 0.12-0.72); the cos i 0.3-0.5 bin (18 galaxies) piles at rank 0 |
